# Lab L2 — parameter overrides
# No parameter overrides beyond SITL defaults.
# All parameters are at their default values.
# The ARMING_CHECK default allows arming without RC in SITL.
# MAVProxy usage: param load l2-first-flight/params.parm
